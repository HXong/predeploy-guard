runtime:
  type: docker-compose

service:
  name: booking-service
  image: nginx:latest
  port: 80
  healthPath: /

checks:
  smoke:
    - name: homepage check
      method: GET
      path: /wrong-path
      expectedStatus: 200

settings:
  cleanup: true
  timeoutSeconds: 60